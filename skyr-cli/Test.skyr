import Random

f = fn(condition) -> String {
  if (condition) {
    id = Random.Identifier {
      name: "one"
      byteLength: 4
    }

    return id.hex
  } else {
    return "hello"
  }
}

debug f(true)
