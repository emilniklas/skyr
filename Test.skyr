import Random
import Google
import List

google = Google {
  project: "emilbroman"
}

List.map(List.range(5), fn(i) {
  google.ServiceAccount {
    accountId: "skyr-test-" + i
    displayName: "Skyr Account"
  }
})
