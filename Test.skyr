import Random
import Google

google = Google {
  project: "emilbroman"
}

id = Random.Identifier {
  name: "network-id"
}

google.ComputeNetwork {
  skyrKey: "my-network"
  name: "my-network-" + id.hex
}
